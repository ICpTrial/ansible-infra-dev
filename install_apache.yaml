# ApacheインストールPlaybook

- hosts: all
  become: true
  tasks:
   - name: yumでApacheインストール
     apt:
       name: apache2
       state: present

   - name: Apacheの起動とchkconfig登録
     service:
       name=httpd
       state=started
       enabled=yes

